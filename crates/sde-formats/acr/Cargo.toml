[package]
name = "sde-acr"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Parser for acr_telemetry's MoTeC-style CSV telemetry export (part of the sde-formats workspace)"

[dependencies]
thiserror = "2"
