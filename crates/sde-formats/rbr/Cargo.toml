[package]
name = "sde-rbr"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Parsers for Richard Burns Rally / RSF companion files — replay metadata, setups, pacenotes (part of the sde-formats workspace)"

[dependencies]
thiserror = "2"
