[package]
name = "tda-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Session/Channel/Lap data model built on top of tda-formats parsers (UI-free)"

[dependencies]
tda-motec = { path = "../tda-formats/motec" }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
