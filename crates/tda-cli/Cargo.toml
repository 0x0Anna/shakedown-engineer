[package]
name = "tda-cli"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "CLI example proving the tda-motec parser -> tda-core data model boundary"

[[bin]]
name = "dump_channels"
path = "src/main.rs"

[dependencies]
tda-core = { path = "../tda-core" }
