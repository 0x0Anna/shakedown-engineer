[package]
name = "sde-setup"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Sim-agnostic car setup sheet model and diff engine, populated by per-sim adapters over sde-formats (UI-free)"

[dependencies]
sde-rbr = { path = "../sde-formats/rbr" }
