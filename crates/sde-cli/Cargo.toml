[package]
name = "sde-cli"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "CLI example proving the sde-motec parser -> sde-core data model boundary"

[[bin]]
name = "dump_channels"
path = "src/main.rs"

[dependencies]
sde-core = { path = "../sde-core" }
