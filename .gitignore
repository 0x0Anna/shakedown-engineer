# Rust
/target
**/target

# Cargo.lock is committed: this workspace produces binaries (sde-cli, sde-app),
# so per Cargo convention we lock the resolved dependency graph.
# If sde-formats/sde-core are split into standalone published library crates
# later, revisit whether their own Cargo.lock should be excluded.

# Editor / IDE
.vs/
.vscode/
*.suo
*.user

# OS cruft
Thumbs.db
.DS_Store

# working directories
.working/**
.work/**

# Sample Log Data
.sample-data/**
